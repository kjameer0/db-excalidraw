// npx nodemon --exec go run main.go --signal SIGTERM
package main

import (
	"fmt"
	"log/slog"
	"net/http"
	"os"
)

// handler function to respond with "Hello, World!"

func helloHandler(count *int) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		*count += 1

		slog.Error("hello", "count", *count)
		fmt.Fprintf(w, "Hello, World!")
	}
}
func main() {
	count := 0
	http.HandleFunc("/", helloHandler(&count)) // route handler
	fmt.Println("Starting server on :8080")
	logger := slog.New(slog.NewJSONHandler(os.Stdout, nil))
	slog.SetDefault(logger)
	// Start the server on port 8080
	if err := http.ListenAndServe(":8080", nil); err != nil {
		fmt.Println("Error starting server:", err)
	}
}
